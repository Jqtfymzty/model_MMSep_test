# 模块二交付件说明

## 1. 提交清单

| 文件                                     | 用途                                            |
| ---------------------------------------- | ----------------------------------------------- |
| `模块二AI测试用例清单.xlsx`            | 15 条正式 AI 测试用例及 Baseline/MMSep 实际结果 |
| `模块二AI测试缺陷清单.docx`            | 仅记录 lcx 负责范围内的 3 个缺陷及回归状态      |
| `模块二AI测试报告.docx`                | 测试对象、策略、环境、结果、反思、贡献和结论    |
| `模块二AI测试演示.pptx`                | 13 页可编辑演示文稿，含原生表格和图表           |
| `自动化测试脚本/`                      | 一键执行 15 条用例或指定用例的交付版入口        |
| `实验记录/module2-formal-0bd7e93-001/` | 输入、原始 JSONL、环境、摘要及 SHA-256 清单     |

## 2. 正式实验结论

- 正式运行编号：`module2-formal-0bd7e93-001`
- 实验源代码：`0bd7e93`
- 指标修正提交：`65d3ea6`
- 15 条用例 × 2 种模式，共 30 个配置结果；无执行错误。
- 输出 Oracle 通过 26/30（86.67%）。
- 把性能阈值计入整条用例后，通过 11/15（73.33%）。
- 公平性和安全性全部通过；功能计数与低对比度计数存在稳定失败。
- MMSep 原型的 KV 估算减少 71.98%–72.98%，质量保持率为 1.00，但解码倍率仅 0.928×、0.948×，未达到 1.2×门槛。
- 最终验收结论：**不通过**。

## 3. 结果边界

本轮使用课程项目自建的 15 条确定性图文夹具，不是 ScienceQA、MMMU、`detail_2k` 或 `complex_reasoning_4k` 的论文基准复现。当前实现是视觉分隔符/KV 压缩原型，没有完整实现论文描述的文本分隔符维护和视觉召回。详细说明见 `docs/EXPERIMENT_SCOPE.md`。

## 4. 证据优先级

1. `raw/results.jsonl` 是不可变的逐次运行记录。
2. `summary/summary.corrected.json` 是修正性能口径后的正式摘要。
3. `summary/summary.json` 与 `manifest.json` 仅作为原始审计记录保留，不再作为性能结论依据。
4. `manifest.corrected.json` 保存本次证据的文件大小和 SHA-256。

## 5. 自动化测试

在项目根目录执行：

```powershell
python 交付件/自动化测试脚本/run_module2_tests.py --help
python 交付件/自动化测试脚本/run_module2_tests.py --run-id module2-formal-001
```

脚本会复用项目正式实验入口并转发筛选参数；完整环境、模型路径与结果目录说明见 `自动化测试脚本/README.md`。本次修改后已通过仓库的 32 项本地自动化回归测试。

## 6. 执行环境迁移说明

原计划在 Ryzen 7 6800H、RTX 3050 4GB、16GB 内存的本地机器执行 LLaVA v1.5 7B 正式对照实验，但该配置无法在统一精度和完整用例规模下可靠完成运行。环境准备阶段曾尝试 Colab 与 Kaggle，最终在获准使用的共享云端/实验室服务器 RTX 4090 上完成正式实验。

为保证证据一致性，服务器运行期间没有拆分提交未完成结果；待运行结束、原始 JSONL、校正摘要、哈希清单和课程交付件全部核验后，才随 `lcx` 分支首次统一上传。`wjn` 的工作使用本地环境，不适用本说明。

## 7. 本地录制材料

`模块二演示视频讲解稿.md` 仅保留在本机供负责人录制，不上传 GitHub。旧版自动生成视频已移入忽略目录 `本地材料/`，最终演示视频由负责人根据 13 页 PPT 和讲解稿重新录制后另行人工审核。
