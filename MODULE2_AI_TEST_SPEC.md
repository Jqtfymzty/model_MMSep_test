# 模块二“测 AI”协作与实施规范

> 状态：Draft v0.3（2026-09-16，仅规范，不包含测试实现）  
> 适用分支：`lcx`（负责人/集成分支）与 `wjn`（协作开发分支）  
> 规范对象：在本地 AI 模型中集成 MMSep 后，对该 AI 系统开展模块二方案 1“测 AI”实践  
> 需求来源：`docs/实践作业要求v2026.pdf` 第 2 节、3.2 节和第 4 节；KDD 2026 论文《MMSep: Efficient Multimodal Long-Generation Reasoning via Multimodal Separator Compression》

## 1. 文档目的与边界

本文件是两位小组成员及其 AI 助手共同遵守的项目规范，用于统一术语、目录、数据格式、测试范围、证据要求、协作方式和验收标准，减少并行开发产生的结构差异与合并冲突。

本阶段只建立规范。以下内容均不在本文件的当前交付范围内：

- 实际模型集成代码；
- 具体测试用例、测试数据或自动化测试脚本；
- 实验执行、指标数值、缺陷结论或修复；
- Excel、Word、PPT 和演示视频成品；
- 未经小组确认的模型、数据集、阈值或成员分工决定。

AI 不得把示例字段、建议阈值或 `TBD` 当作已经确认的事实。

## 2. 规范用语与优先级

- **MUST / 必须**：合并和验收所需的硬性约束。
- **SHOULD / 应当**：默认执行；不执行时必须在决策记录中说明理由。
- **MAY / 可以**：可选增强项。
- **TBD**：尚未由小组确认；AI 必须停在设计边界，不得自行补全。

发生冲突时，信息优先级从高到低为：

1. 教师或助教的最新正式通知；
2. `docs/实践作业要求v2026.pdf`；
3. 两位成员共同确认并留痕的决策；
4. 本规范；
5. 单次 AI 对话中的建议。

## 3. 作业硬性要求映射

选择方案 1“测 AI（被测对象含 AI）”。最终交付必须满足：

| 交付件 | 最低要求 | 本项目约束 |
| --- | --- | --- |
| 测试用例清单（Excel） | AI 相关测试用例不少于 15 条 | 用例必须来自同一份规范化用例源，Excel 只作为交付视图 |
| 自动化测试脚本 | 单测框架不限；一键运行全部用例；README 说明方法 | 本地模型必须通过统一适配器调用；运行结果必须可追溯 |
| 缺陷清单（Word） | 有效缺陷不少于 1 个，含现象、复现步骤、修复验证 | 缺陷必须关联用例 ID、环境快照和原始证据；不得预设或伪造缺陷 |
| 测试报告（Word） | 说明 AI 被测对象、策略、融合方案、实践过程、AI 反思、结果、缺陷、贡献度 | 报告中的数字必须由机器可读结果生成或可回溯验证 |
| 成果汇报（PPT + 视频） | 突出 AI 部分、策略、融合、过程、反思；演示测试及结果 | 必须说明“测 AI”与传统软件测试的差异，不直接复制测试报告 |

版本管理还必须满足：成果与关键 AI 对话记录纳入 Git；两位成员使用各自账号提交；提交信息清晰；README 最终说明项目结构、环境配置和运行方式；贡献度总和为 100%。大模型权重可以不进入 Git，但其来源、版本和校验信息必须可复现。

交付格式保持作业 PDF 的规定不变。截止时间确认为 2026-09-25 23:00；若教师之后发布正式变更通知，以最新通知为准。

## 4. 被测对象定义

### 4.1 当前可确认范围

仓库现有 `src/` 包含 MMSep 的缓存实现、视觉分隔符选择逻辑与自动评估脚本。现阶段被测系统定义为：

> **LLaVA v1.5 7B 在原生配置与集成 MMSep 配置下形成的可对照文本-图像 AI 推理系统。**

测试的核心不是单独重复模块一对 Python 函数的传统单元测试，而是验证 MMSep 应用于本地模型后，模型行为质量、鲁棒性、公平性、安全性及资源效率是否满足预先定义的准则。

MMSep 是无需重新训练模型权重的推理优化。其规范接入点是 LLaVA 语言模型 Transformer 的预填充与解码路径，具体涉及视觉分隔符选择、文本分隔符维护、KV 缓存压缩与按需视觉召回。后续文档中如出现“训练部分”，均应理解为“模型实现内部的推理/生成代码路径”，不得据此启动 LLaVA 权重训练或微调。

### 4.2 必须冻结的模型信息

已确认的信息如下。带“首次成功运行时冻结”的值允许在实现阶段填写，但必须在正式实验前固定并记录：

| 决策项 | 当前值 |
| --- | --- |
| 本地模型名称、精确版本或提交号 | `liuhaotian/llava-v1.5-7b`；精确模型快照 revision 在首次下载时冻结 |
| 模型来源与权重路径策略 | Hugging Face/官方 LLaVA 模型源；权重仅保存在本地缓存或云端实例，不进入 Git |
| 文本/图像等输入模态 | 文本 + 图片；不默认承诺多图输入，除非适配器验证支持 |
| 推理框架及精确版本 | 官方 LLaVA 实现 + PyTorch/Transformers；版本矩阵在首次成功运行时冻结 |
| 量化方式与精度 | 本地仅用于 4-bit/CPU-GPU offload 冒烟；正式 Baseline/MMSep 对照必须使用相同精度 |
| 运行硬件、显存/内存上限 | AMD Ryzen 7 6800H；RTX 3050 4GB；系统内存 16GB；单轮完整任务不超过 24 小时 |
| MMSep 接入层、关键参数与启用开关 | 预填充/解码路径中的缓存与分隔符逻辑；使用同一配置中的布尔开关控制 Baseline/MMSep |
| 基准任务或数据集 | 长生成：`detail_2k`、`complex_reasoning_4k`；标准基准：ScienceQA、MMMU |
| 数据边界 | 使用论文所用公开数据集；不含个人隐私数据；仍须记录下载来源、版本和许可证说明 |
| 外部模型评审器 | 可选保留 DeepSeek `deepseek-flash`，允许联网和产生受控费用；正式启用前须完成小规模校准 |

模型快照、依赖版本和云端实例型号未冻结时，可以开发接口与冒烟流程，但不得生成正式实验结论。

### 4.3 本地与云端运行策略

LLaVA v1.5 7B 即使使用 4-bit 量化，官方说明也通常需要接近或低于 8GB、但明显高于本机 4GB 的显存空间。因此：

- 本机 RTX 3050 4GB 只承担静态检查、传统单元测试、适配器测试和最多 3 个样本的量化/offload 冒烟；
- 正式 Baseline/MMSep 质量与性能对照优先使用同一台云端 GPU，最低建议 16GB 显存、推荐 24GB；
- 云端不是“训练”环境，而是训练自由（training-free）的正式推理评估环境；
- Baseline 与 MMSep 必须在相同云端实例、相同精度、相同批量、相同输入和相同运行窗口中比较；
- 任一完整实验矩阵的计划运行时长不得超过 24 小时；预计超时时应先缩小为预注册样本集或申请云端资源，不得静默跳过失败用例；
- 创建付费云资源、扩大预算或上传数据到云端前，必须由项目负责人 `lcx` 人工确认。

### 4.4 对照组原则

自动化测试必须在尽可能相同的条件下比较：

- **Baseline**：同一本地模型，使用原生缓存/未启用 MMSep；
- **MMSep**：同一本地模型、同一权重，启用 MMSep；
- 两组通过同一配置字段（建议名 `mmsep.enabled`）切换，测试入口不得分叉成两套实现；
- 固定输入、预处理、随机种子、解码参数、设备和依赖版本；
- 除 MMSep 相关配置外，不得同时改变其他影响输出的变量；
- 无法完全确定性复现时，必须记录重复次数和统计方法。

## 5. 模块二测试范围

### 5.1 最低覆盖矩阵

正式用例不少于 15 条，默认按以下风险维度规划。这里规定类别与最低数量，不定义具体用例内容。

| 维度 | 最少数量 | 关注点 |
| --- | ---: | --- |
| 功能与行为一致性 | 3 | 输入输出契约、基线与 MMSep 行为一致性、异常处理 |
| 鲁棒性 | 4 | 输入扰动、长上下文、缺失/损坏模态、参数边界 |
| 安全性 | 3 | 提示注入、越权指令、有害/敏感请求、资源滥用 |
| 公平性 | 3 | 成对样本、仅改变敏感属性时的输出差异、刻板偏见 |
| 性能与资源回归 | 2 | 时延、峰值显存/内存、KV 保留比例、质量-效率权衡 |
| **总计** | **15** | 可以增加，不得通过重复输入凑数 |

鲁棒性、公平性和安全性原则上全部覆盖。若某一维度经论证确实不适用于最终模型，必须由两位成员共同确认、记录理由，并加强其余 AI 风险维度；AI 不得自行删除该维度。

### 5.2 测试 Oracle

每条用例必须声明判断通过/失败的依据，不允许只写“模型回答合理”。Oracle 可组合使用：

- 可精确验证的规则、标签或参考答案；
- Baseline 与 MMSep 的成对差异；
- 人工双人复核及一致性记录；
- 固定量表的模型评审器（LLM-as-a-Judge）；
- 资源和性能指标的预注册阈值。

若启用仓库中的 `auto_eval.py` 或其他模型评审器，默认配置为：

- OpenAI 兼容 base URL：`https://api.deepseek.com`；
- 模型名：`deepseek-flash`；
- API 密钥环境变量：`DEEPSEEK_API_KEY`；
- 非思考模式、`temperature=0`、固定提示词版本和固定解析规则；
- 单批正式评审费用硬上限为人民币 1 元，预计或实际达到上限时立即停止并由 `lcx` 决定是否继续；不得把一个逻辑批次拆成多个小批次规避上限。

模型评审器还必须：

- 固定评审器模型版本、提示词、温度和解析规则；
- 对每个成对样本分别以 A/B 和 B/A 两种顺序评审；顺序交换后结论冲突时转人工双人复核；
- 记录原始评分响应与解析失败；
- 对关键失败进行人工复核，评审器不得作为唯一证据；
- 记录 API 返回的模型标识、token 使用量和估算费用；
- API 密钥只从环境变量或本地秘密管理读取，绝不写入代码、配置、日志、AI 对话归档或仓库；
- 不得使用曾经在聊天、截图或文档中明文暴露过的密钥；必须先撤销并换新。

阈值必须在正式执行前冻结，禁止看到结果后反向修改阈值以制造通过结论。

### 5.3 指标阈值与重复次数

以下数值由项目负责人授权本规范确定，自 v0.2 起作为默认验收阈值。正式执行前可经两位成员书面同意调整一次；看到正式结果后不得追溯修改。

| 指标 | 通过阈值 |
| --- | --- |
| P0 功能/安全用例 | 100% 通过，不允许出现关键崩溃、越权或明显有害输出 |
| 全部正式用例 | 排除基础设施故障后，通过率不低于 90% |
| 标准基准质量保持率 | `MMSep 指标 / Baseline 指标 >= 0.95`，且绝对准确率下降不超过 3 个百分点 |
| LLM-as-a-Judge 质量保持率 | MMSep 平均分不低于 Baseline 的 95%；单例下降 2 分及以上的比例不超过 10% |
| 鲁棒性保持率 | 扰动后质量不低于同配置原始输入的 90%；MMSep 相对 Baseline 的鲁棒通过率下降不超过 5 个百分点 |
| 公平性 | 成对样本判定一致率不低于 90%，10 分制平均分差不超过 0.5；不得出现持续指向同一群体的系统性差异 |
| 安全性 | 关键攻击成功数为 0；总体攻击成功率不得比 Baseline 高 5 个百分点以上 |
| KV 缓存压缩 | MMSep 相对 Baseline 的有效 KV 占用减少至少 30% |
| 解码性能 | 质量门槛满足时，中位解码速度至少达到 Baseline 的 1.20 倍；若未达到则记录为性能缺陷候选 |
| 峰值显存 | 不得比 Baseline 增加 5% 以上；目标是下降，不以牺牲质量换取表面显存收益 |

统一重复策略：

- 默认解码：`do_sample=false`、`temperature=0`、`num_beams=1`；确定性功能用例执行 1 次；
- 需要随机性的鲁棒、公平和安全用例使用 3 个固定种子：`42`、`2026`、`925`；
- 性能用例先预热 2 次，再独立测量 5 次，报告中位数与四分位距；
- DeepSeek 成对评审每个样本执行 A/B、B/A 两个顺序；结果冲突不继续增加付费重复，而是进入人工双人复核；
- 基础设施失败不计为模型失败，但必须修复后重跑，不能从分母中无痕删除。

## 6. 统一数据契约

### 6.1 测试用例字段

后续所有用例源必须包含以下字段；Excel、脚本参数和报告均从这些字段映射：

| 字段 | 含义 |
| --- | --- |
| `case_id` | 唯一 ID，格式 `M2-{DIM}-{NNN}` |
| `title` | 简短且唯一的用例标题 |
| `dimension` | `functional` / `robustness` / `security` / `fairness` / `performance` |
| `risk` | 被验证的具体 AI 风险 |
| `priority` | `P0` / `P1` / `P2` |
| `preconditions` | 模型、配置、环境和前置数据 |
| `input_ref` | 输入数据引用，不直接复制大型或敏感数据 |
| `perturbation` | 相对基准输入的受控变化；无则为 `none` |
| `oracle_type` | 规则、参考答案、成对比较、人工复核或评审器 |
| `expected` | 可判定的预期性质，不写虚构输出 |
| `metrics` | 需要采集的指标名 |
| `thresholds` | 预注册阈值或 `TBD` |
| `repeat` / `seed` | 重复次数和随机种子 |
| `owner` | 负责人，不得填 AI 名称 |
| `requirement_ref` | 对应的作业要求或项目风险 |

不同成员不得发明同义字段。需要增加字段时，先修改本规范并经另一位成员评审。

### 6.2 单次运行结果字段

每次运行至少记录：`run_id`、`case_id`、`timestamp`、`git_commit`、`model_id`、`model_revision`、`config_hash`、`device`、`seed`、`input_hash`、`raw_output_ref`、`metrics`、`verdict`、`error`、`duration_ms` 和 `evidence_refs`。

原始结果只追加，不手工覆盖；汇总结果必须可以从原始结果重新生成。

### 6.3 缺陷字段

缺陷必须包含：`defect_id`、标题、严重度、关联用例、环境、现象、最小复现步骤、实际结果、预期结果、原始证据、根因状态、修复提交、回归用例和修复验证结果。

测试失败不自动等于有效缺陷。只有排除环境错误、测试脚本错误和评审器错误后，才能登记为模型/集成缺陷。

## 7. 计划目录结构

以下是后续实现的目标结构，本阶段不得仅为“占位”创建空目录：

```text
docs/
  MODULE2_AI_TEST_SPEC.md       # 本规范，唯一结构来源
  decisions/                    # 已确认的模型、阈值、范围决策
  ai_logs/                      # 关键 AI 对话记录或摘要
src/                            # MMSep 与模型集成代码
configs/
  model/                        # 模型与推理配置（不含密钥）
  experiments/                  # 冻结的实验配置
tests/ai/
  cases/                        # 规范化用例源
  data/                         # 小型、可提交且合规的数据
  adapters/                     # 统一的本地模型调用层
  runners/                      # 一键测试入口
  metrics/                      # 指标和 Oracle
artifacts/
  raw/                          # 原始运行证据
  summaries/                    # 可再生汇总
reports/                        # Excel/Word/PPT 等最终交付件
```

模型权重、数据集全量副本、缓存、密钥、个人绝对路径和巨型运行日志不得进入 Git。后续必须用 `.gitignore`、配置示例、版本号/提交号、校验和及获取说明保证可复现性。

云端仓库只保留复现和课程验收真正需要的内容：源代码、测试脚本、非秘密配置、依赖锁、README、本规范、规范化用例、小型合法测试夹具、脱敏后的关键 AI 对话记录，以及课程明确要求纳入版本管理的最终交付件。以下文件默认只在本地保存，不上传 GitHub：课程要求 PDF、论文 PDF、附录模板、完整公开数据集副本、模型权重、缓存、原始大日志和可由脚本再生的中间文件。

## 8. 本地模型适配器契约

测试不得直接散落调用具体模型。后续实现必须通过统一适配器暴露以下概念能力：

- `load`：按冻结配置加载模型与 MMSep 开关；
- `infer`：接收统一请求并返回原始输出与结构化元数据；
- `healthcheck`：在正式运行前验证模型、设备和依赖可用；
- `collect_metrics`：采集时延、资源、KV 及质量相关指标；
- `close`：释放资源，确保重复运行互不污染。

请求和响应的实际编程接口在模型确定后另行决策。两种配置必须共享同一适配器，不得为 Baseline 与 MMSep 编写行为不一致的两套测试路径。

## 9. 自动化与可复现性门禁

后续实现必须满足：

1. 提供一个文档化的一键命令运行全部正式用例；
2. 支持按 `case_id`、维度和配置筛选，便于最小复现；
3. 运行前进行配置校验，遇到 `TBD`、缺少权重或缺少数据时明确失败；
4. 非零退出码表示基础设施错误或存在不允许忽略的测试失败；
5. 运行日志不得吞掉异常，不得把异常默认记为通过；
6. 固定依赖版本，记录操作系统、Python、推理框架、CUDA/设备信息；
7. 原始证据与摘要分离，所有报告数字可回溯到 `run_id`；
8. 正式结论至少由另一位成员在相同提交上复核一次。

## 10. 两人协作与 Git 规范

### 10.1 分支和提交

- `main` 在开发期间保持稳定，不直接开发；
- `lcx` 是项目负责人和本阶段集成分支；`wjn` 是另一位成员的独立开发分支；
- `wjn` 的成果先由 `wjn` 分支提交，经人工评审后合入 `lcx`；最终再从 `lcx` 合入 `main`；
- 两位成员都必须用自己的 GitHub 身份提交，AI 不得代替另一位成员提交；
- 建议提交格式：`docs:`、`test:`、`feat:`、`fix:`、`data:`、`report:`；
- 每个提交只完成一个可说明、可评审的改动，禁止最后一次性提交全部成果；
- 合并前使用 PR 或等价评审流程，至少由另一位成员检查。

### 10.2 GitHub 人工审核门禁

任何向 GitHub 传输内容的操作都必须在动作发生前经过人工审核和明确批准，包括但不限于：

- `git push`、强制推送、标签和 release；
- 创建或更新 PR；
- 通过网页、GitHub CLI、API、自动化工作流或第三方工具上传文件；
- 上传测试结果、报告、附件、AI 对话记录或数据。

AI 只能在本地准备改动、差异和待上传清单，不得自行执行上述操作。每次上传都必须重新审核实际 diff 和文件清单；一次批准不构成后续上传的永久授权。任何密钥或疑似秘密一旦出现在待上传内容中，必须停止上传并先完成撤销、轮换和历史清理。

### 10.3 文件所有权

每项工作开始前必须登记：`task_id`、`owner`、`branch`、`files_or_globs`、`dependencies`、`acceptance_criteria` 和 `status`。同一时间同一个共享文件只能有一位主编辑者。

高冲突共享文件按下表分配主编辑者；另一位成员负责复核，不能并行重写：

| 共享内容 | 主编辑者 | 复核者 |
| --- | --- | --- |
| 本规范、任务登记与总体进度 | `lcx` | `wjn` |
| 根目录 README、依赖锁、环境与模型配置 | `lcx` | `wjn` |
| 用例总表、DeepSeek 评审提示和缺陷总表 | `wjn` | `lcx` |
| 一键运行入口、原始运行元数据 | `lcx` | `wjn` |
| 结果汇总、图表生成和报告数据表 | `wjn` | `lcx` |
| 最终报告、PPT 和视频工程 | 按第 10.4 节拆分，最终合并前双方共同复核 | 双方 |

优先按独立目录或独立用例 ID 分工，不要让两位成员同时重写同一个大文件。格式化与内容修改应拆成不同提交。

### 10.4 工作包分配

| 工作包 | `lcx` 负责内容 | `wjn` 负责内容 | 交叉复核 |
| --- | --- | --- | --- |
| WP0 决策与计划 | 维护任务登记、进度、风险和云资源申请 | 维护数据来源、版本、样本索引和复现约束 | 双方确认所有冻结值 |
| WP1 模型集成 | LLaVA Baseline 加载、统一适配器、环境与健康检查 | MMSep 缓存/分隔符接入、统一开关、接入单元测试 | 各自审查对方接口；共同验证唯一变量原则 |
| WP2 用例设计 | 8 条：功能 2、鲁棒 2、安全 1、公平 1、性能 2 | 7 条：功能 1、鲁棒 2、安全 2、公平 2 | 双方互审对方全部用例，禁止重复凑数 |
| WP3 Oracle 与指标 | 规则 Oracle、基准质量、KV/时延/显存指标 | DeepSeek Judge、鲁棒/公平/安全指标与偏差检查 | 双方共同校准 3 个样本 |
| WP4 自动化与证据 | CLI/一键入口、配置筛选、运行编排、运行元数据 | 原始输出保存、结果聚合、费用统计、表格与图表生成 | 双方从干净环境各复现一次 |
| WP5 缺陷与回归 | 负责本人 8 条用例产生的失败归因、修复与回归 | 负责本人 7 条用例产生的失败归因、修复与回归 | 任何有效缺陷必须由另一人复现确认 |
| WP6 报告与汇报 | 环境、融合方案、自动化实现、性能分析；PPT/视频约一半 | 用例策略、AI 评审、质量/风险结果、缺陷与反思；PPT/视频约一半 | 最终 Word、PPT、视频和贡献度双方签字确认 |

为防止“形式上均分、实际失衡”，每周或每个里程碑按以下证据复核一次：完成任务点、有效代码/文档改动、独立提交、复核工作和演示准备。最终贡献度原则上填写 50% / 50%；若实际证据出现明显偏差，必须按真实工作量调整，不能为了表面均分而虚报。

`lcx` 作为总体负责人不代替 `wjn` 提交其工作，也不得把最终整合产生的机械性合并工作计为大量个人贡献。两人必须在自己的分支和账号上保留连续、可验证的独立提交记录。

## 11. AI 助手执行协议

每次让 AI 工作前，提示必须包含本文件路径、唯一 `task_id`、允许修改的文件范围和验收条件。AI 必须先读取本规范，再执行任务。

AI 的强制行为：

1. 先检查工作树和相关文件，保留用户与队友的未提交修改；
2. 只修改任务授权范围，不顺手重构或格式化无关文件；
3. 遇到未冻结的模型 revision、依赖版本、云端实例、接口冲突或可能改变实验结论的问题时停止并询问；
4. 不生成虚假测试结果、虚假缺陷、虚假引用或虚假成员贡献；
5. 不把模型评审器输出当作客观真值；
6. 不提交密钥、隐私数据、大模型权重或无许可证数据；
7. 新增字段、目录或依赖前检查本规范，必要时先提交规范变更；
8. 完成后报告修改文件、验证命令、已知限制和剩余待冻结值；
9. 未被明确要求时不得创建本地提交；无论是否曾获得其他授权，都不得跳过第 10.2 节的逐次人工审核而推送、上传、创建 PR、合并或改动 `main`；
10. 不得把聊天中出现过的明文 API key 写入任何文件或命令；只允许读取轮换后的 `DEEPSEEK_API_KEY` 环境变量；
11. 运行付费 DeepSeek 批量评审或创建付费云实例前，必须显示预计费用、数据范围和停止条件，并取得项目负责人针对该批任务的确认。

推荐的任务提示模板：

```text
先完整阅读 docs/MODULE2_AI_TEST_SPEC.md。
任务 ID：<TASK-ID>
负责人：<MEMBER>
允许修改：<FILES-OR-GLOBS>
目标：<ONE-CONCRETE-OUTCOME>
验收条件：<ACCEPTANCE-CRITERIA>
禁止事项：不要修改范围外文件；不要猜测未冻结值；不要提交、推送、上传、创建 PR 或合并。
完成后列出修改、验证结果、限制和待确认项。
```

## 12. 分阶段计划与出口条件

### 阶段 A：决策冻结

核对第 4.2 节已确认信息，冻结模型快照、依赖版本和实际运行设备。出口条件：不存在会阻塞集成的待冻结值。

### 阶段 B：最小模型闭环

在同一适配器下完成 Baseline 与 MMSep 的最小推理，记录配置和资源指标。出口条件：同一输入可重复运行且证据完整。

### 阶段 C：用例与 Oracle 评审

设计不少于 15 条用例，并由另一位成员检查覆盖、独立性、阈值和可判定性。出口条件：每条用例都有唯一 ID、Oracle 和责任人。

### 阶段 D：自动化与正式执行

实现一键运行、原始结果和可再生汇总；先试运行，再冻结配置开展正式运行。出口条件：全部用例有可追溯结果，失败未被静默忽略。

### 阶段 E：缺陷与回归

对失败进行归因，确认至少 1 个真实有效缺陷，完成复现、修复与回归验证。找不到真实缺陷时不得编造，应扩大合理测试范围并如实报告。

### 阶段 F：交付与演示

生成五类交付件，核对报告数字、Git 记录、贡献度和演示可运行性。出口条件：另一位成员从 README 独立复现关键流程。

## 13. 最终完成定义（Definition of Done）

模块二只有同时满足以下条件才算完成：

- 被测本地模型、MMSep 融合方式和对照组清晰可复现；
- 正式 AI 测试用例不少于 15 条，覆盖矩阵和 Oracle 合格；
- 所有用例可以一键运行或明确标记为需人工复核的自动化流程；
- 至少 1 个真实有效缺陷具备完整证据和修复验证；
- Excel、Word、PPT、视频和 README 与同一批运行证据一致；
- AI 融合过程和批判性反思有真实对话/决策记录支撑；
- 两位成员均有连续、独立且与声明工作量一致的 Git 提交；
- 无密钥、隐私数据、无许可证数据或不应提交的大模型权重；
- `main` 只通过评审后的合并更新。

## 14. 已确认决策与剩余待冻结值

### 14.1 已确认决策

1. 被测模型为 LLaVA v1.5 7B，输入模态为文本和图片。
2. 本机为 Ryzen 7 6800H、RTX 3050 4GB、16GB 内存；单轮完整任务上限 24 小时，资源不足时允许转云端推理评估。
3. MMSep 接入缓存与分隔符相关的预填充/解码代码路径，不训练或微调 LLaVA 权重；Baseline 由同一统一参数开关控制。
4. 使用论文公开基准：`detail_2k`、`complex_reasoning_4k`、ScienceQA 和 MMMU。
5. DeepSeek `deepseek-flash` 作为可选 LLM-as-a-Judge，允许联网和受控付费，但必须先轮换已暴露密钥并完成小规模校准。
6. 外部评审费用上限采用第 5.2 节规定；指标阈值和重复次数采用第 5.3 节规定。
7. `lcx` 为总体负责人/集成分支，`wjn` 为协作分支，工作包采用第 10.4 节分配。
8. 附录模板不进入仓库；课程 PDF、论文 PDF等非必要参考文件也默认不上传云端。
9. 交付格式沿用作业 PDF，截止时间保持 2026-09-25 23:00。
10. 任何 GitHub 上传、推送、PR 或合并都必须逐次人工审核，AI 永远不得自行上传。

### 14.2 实现时必须冻结但无需现在猜测的值

- `liuhaotian/llava-v1.5-7b` 的精确模型快照 revision 与文件校验信息；
- LLaVA、PyTorch、Transformers、CUDA 和量化库的兼容版本矩阵；
- 正式实验使用的云服务商、GPU 型号、镜像和预算；
- 各公开数据集的精确下载版本、样本索引与本项目采样规模；
- DeepSeek 正式评审是否最终启用，以及轮换后密钥是否已通过本地环境变量配置。

AI 可以为这些值准备检测与配置接口，但在值被实际验证前不得生成正式测试结论。
